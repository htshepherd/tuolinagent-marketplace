# Outreach Agent Placeholder

未来只消费 `product_pack`、`competitor_pack`、`content_pack` 和 `evidence_pack`。

边界：

- 不直接读取完整 Graphify `graph.json`。
- 不默认读取真实 `raw/`。
- 可以使用待复核素材生成开发信或跟进信草稿，但必须保留复核标记。
- 首版不生成最终可发送内容，只预留未来接入边界。
