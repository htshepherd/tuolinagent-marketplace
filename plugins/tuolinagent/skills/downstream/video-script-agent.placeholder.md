# Video Script Agent Placeholder

未来只消费 `product_pack`、`content_pack`、`video_pack` 和 `evidence_pack`。

边界：

- 不直接读取完整 Graphify `graph.json`。
- 不默认读取真实 `raw/`。
- 可以使用待复核素材生成分镜或 prompt 草稿，但必须保留复核标记。
- 首版不生成最终即梦 prompt，只预留未来接入边界。
